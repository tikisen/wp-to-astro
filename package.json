{
  "name": "wp-to-astro",
  "type": "module",
  "version": "0.1.0",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "extract-wp": "op run --env-file=.env.template -- node scripts/extract-wp.js",
    "crawl": "op run --env-file=.env.template -- node scripts/crawl.js",
    "screenshot": "op run --env-file=.env.template -- node scripts/screenshot.js",
    "extract-styles": "op run --env-file=.env.template -- node scripts/extract-styles.js",
    "download-assets": "op run --env-file=.env.template -- node scripts/download-assets.js",
    "component-inventory": "op run --env-file=.env.template -- node scripts/component-inventory.js",
    "qa:visual": "op run --env-file=.env.template -- node scripts/qa-visual.js",
    "extract:all": "npm run extract-wp && npm run crawl && npm run screenshot && npm run extract-styles && npm run download-assets && npm run component-inventory"
  },
  "dependencies": {
    "@mendable/firecrawl-js": "^4.18.1",
    "cheerio": "^1.0.0",
    "css-tree": "^3.0.0",
    "playwright": "^1.50.0"
  },
  "devDependencies": {
    "vitest": "^3.0.0"
  }
}
